[global]
index-url = https://pypi.org/simple

[install]
# Ignore package versions uploaded less than 3 days ago (supply chain protection).
# Requires pip >= 26.0. No-op on older versions.
uploaded-prior-to = P3D
